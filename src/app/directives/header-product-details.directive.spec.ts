import { HeaderProductDetailsDirective } from './header-product-details.directive';

describe('HeaderProductDetailsDirective', () => {
  it('should create an instance', () => {
    const directive = new HeaderProductDetailsDirective();
    expect(directive).toBeTruthy();
  });
});
