import { HeaderProductDirective } from './header-product.directive';

describe('HeaderProductDirective', () => {
  it('should create an instance', () => {
    const directive = new HeaderProductDirective();
    expect(directive).toBeTruthy();
  });
});
